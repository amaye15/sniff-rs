[package]
name = "sniff-rs"
version = "0.1.0"
edition = "2024"

[features]
default = []
# parquet also covers Arrow IPC/Feather (.arrow/.feather) - both are Arrow-ecosystem
# formats, and both now have their own hand-rolled reader (parquet_support/
# arrow_ipc_support in src/lib.rs) rather than reading through the arrow/parquet
# crates directly - see CLAUDE.md's Dependency footprint section for the full
# writeup. No dep: entries needed here any more: this feature now gates only
# this project's own code, the same shape every other hand-rolled format's
# feature flag already has.
parquet = []
xlsx = []
avro = []
full = ["parquet", "xlsx", "avro", "sqlite", "msgpack", "toml", "yaml", "cbor", "ini", "xml", "zstd", "npy", "weblog", "syslog", "dbase", "stata", "sas7bdat", "spss", "orc", "bson", "plist", "json5", "har", "geojson", "mbox", "vcard", "icalendar", "delta", "iceberg"]
sqlite = []
msgpack = []
toml = []
yaml = []
cbor = []
ini = []
xml = []
zstd = []
npy = []
weblog = []
syslog = []
dbase = []
stata = []
sas7bdat = []
spss = []
orc = []
# Hand-rolled, no dep - see src/lib.rs's bson_support for the writeup and
# CLAUDE.md's Dependency footprint section. The real `bson` crate is a
# dev-only cross-verification oracle (see [dev-dependencies]).
bson = []
# Hand-rolled, no dep - both the XML plist and binary plist (bplist00)
# variants live in src/lib.rs's plist_support.
plist = []
# Hand-rolled, no dep - a deliberately independent relaxed-JSON parser
# (comments, trailing commas, unquoted keys, single-quoted strings),
# separate from the core json_support parser on purpose - see
# json5_support's own doc comment in src/lib.rs. Covers both .json5 and
# .jsonc, since .jsonc's own grammar is a strict subset.
json5 = []
# Hand-rolled, no dep - HAR is standard JSON with a fixed top-level
# shape, so har_support just reuses the always-on json_support parser
# and extracts log.entries as the records array.
har = []
# Hand-rolled, no dep - GeoJSON is standard JSON with a fixed top-level
# shape too, so geojson_support reuses the always-on json_support
# parser and extracts a FeatureCollection's own features array.
geojson = []
# Hand-rolled, no dep - a hand-rolled RFC 4155 mbox reader
# (mbox_support): message-boundary detection plus a real RFC 822
# header parser (folded headers, repeated headers pooled into arrays).
mbox = []
# Hand-rolled, no dep - a hand-rolled RFC 6350 vCard property-line
# parser (vcard_support), sharing its folded-content-line grammar with
# icalendar below but scoped to vCard's own BEGIN:VCARD/END:VCARD
# records.
vcard = []
# Hand-rolled, no dep - a hand-rolled RFC 5545 iCalendar property-line
# parser (ical_support), one record per VEVENT/VTODO component.
icalendar = []
# Delta Lake table awareness (delta_support): auto-detected directly from
# the input path being a directory containing `_delta_log/` - resolves
# the transaction log's own JSON commits to the table's current schema
# and live set of Parquet data files, then profiles them as one logical
# table by reusing the already-hand-rolled `parquet` reader's own row-
# decode primitive. No new dependency and no new binary format of its
# own - Delta's transaction log is plain JSON (this project's always-on
# core parser already reads it) and its data files are exactly the
# Parquet this project already reads; `delta` therefore always pulls in
# `parquet` too, the same way `orc`'s own LZ4 codec reuse implies no new
# feature of its own. See src/lib.rs's delta_support module and
# CLAUDE.md's own "Lakehouse table formats" section for the full design -
# checkpoint files, delta.columnMapping.mode, and deletion vectors are
# all read now (a later pass closed all three); no catalog integration
# remains a deliberate, permanent boundary, not a gap.
delta = ["parquet"]
# Apache Iceberg table awareness (iceberg_support): auto-detected directly
# from the input path being a directory containing a `metadata/`
# subdirectory with at least one real `*.metadata.json` file. No new
# binary format either: Iceberg's own metadata chain is JSON (the current
# metadata.json) plus Avro (the current snapshot's manifest-list, and
# each manifest file it names) plus Parquet (the live data files those
# manifests name) - three formats this project already hand-rolled, so
# `iceberg` pulls in both `avro` and `parquet` rather than being its own
# new decoder. Unlike Delta's single flat JSON commit log, resolving
# Iceberg's current live-file set is a genuine multi-hop chain (metadata
# JSON -> current snapshot -> manifest-list Avro -> manifest Avro files ->
# live data files) - see src/lib.rs's iceberg_support module and
# CLAUDE.md's own "Lakehouse table formats" section for the full design,
# including the disclosed scope boundary (delete files/manifests, schema
# evolution across snapshots, and non-Parquet data files aren't read yet).
iceberg = ["avro", "parquet"]
# Portable SIMD (`std::simd`, RFC 2977) acceleration for this project's
# own hottest, most heavily hand-optimized byte-scanning loop (the CSV
# reader's own field/quoted-field scan) - genuinely optional and off by
# default, since `std::simd` is still unstable (nightly-only, no
# committed stabilization timeline as of this writing) and this project
# otherwise builds and runs entirely on stable Rust. No new dependency:
# `simd_support` in src/lib.rs is pure `core::simd`, the standard
# library's own SIMD API, not a third-party crate. Requires `cargo
# +nightly build --release --features simd` explicitly - never implied
# by `full`, and never required for anything else in this project. See
# src/lib.rs's `simd_support` module and CLAUDE.md's own "Performance"
# section for the full design, the real, measured verdict on whether it
# actually helps, and why.
simd = []

[dev-dependencies]
# Unlike every other entry in this list, `serde`/`serde_json` used to be
# this project's last two *unconditional* runtime dependencies - the ones
# CLAUDE.md long documented as "meant to stay a dependency permanently",
# specifically because `serde_json::Value` is the shared recursive bridge
# type seven different format readers (JSON, YAML, TOML, Avro, MessagePack,
# CBOR, XML) all flatten through, and because reimplementing the full
# `Serializer` trait surface looked disproportionate to the ~6 real calls
# that ever used it. Both claims turned out not to hold: `json_support` in
# src/lib.rs is a hand-rolled `Value`/`Number`/`Map`, a from-scratch
# recursive-descent parser, and a pretty-printer, all built the same
# "read the real crate's source as the spec" way as every other hand-roll
# in this list - and the two hand-written `impl serde::Serialize` blocks
# (`ColumnProfile`, a local `DataDictionary` wrapper) became plain inherent
# `to_json(&self) -> Value` methods instead, so `serde`'s own `Serializer`
# trait was never actually needed. Kept only as this project's own
# cross-verification oracle now: `tests/integration.rs` still parses this
# tool's JSON output through the real, independent `serde_json` parser
# (a genuinely stronger conformance proof than re-parsing with the same
# hand-rolled reader that produced the output), and
# `json_reader_matches_the_serde_json_crate_output_exactly` /
# `pretty_output_round_trips_through_real_serde_json` in src/lib.rs's own
# `#[cfg(test)]` blocks compare directly against it. One deliberate,
# disclosed behavior change came out of this rewrite: the new `Map` is
# insertion-ordered (a `Vec<(String, Value)>`), not `BTreeMap`-backed like
# `serde_json::Map` without its own `preserve_order` feature - so nested
# JSON/YAML/TOML/Avro/MessagePack/CBOR/XML output now surfaces columns in
# their natural source order instead of always alphabetized, a real fix to
# a long-standing accidental side effect a nearby doc comment already
# claimed shouldn't have been happening. The rewrite also caught a real,
# live, previously-untested bug along the way: `yaml_support`'s handling of
# a bare `.inf`/`-.inf` YAML scalar called
# `serde_json::Number::from_f64(f64::INFINITY).unwrap()`, which panics
# with the real crate too (`from_f64` returns `None` for non-finite input)
# - fixed to fall back to the literal string, matching the adjacent `.nan`
# arm's own already-correct treatment.
serde_json = "1.0.151"
serde = "1.0.229"
# Dev-only - never affects the shipped binary or a consumer's build, unlike
# every other dependency decision in this file. See CLAUDE.md's benchmarking
# section for why this is the one place this project reaches for a big,
# full-featured crate instead of hand-rolling something leaner.
criterion = { version = "0.8.2", features = ["html_reports"] }
# Same dev-only treatment, same reason, at by far the largest scale of any
# crate in this list: the `parquet` feature's own hand-rolled readers
# (parquet_support/arrow_ipc_support in src/lib.rs - Thrift-encoded footer
# metadata, every compression codec but LZO, every encoding, nested Struct/
# List/Map/Union/RunEndEncoded reconstruction, and a from-scratch FlatBuffers
# reader for Arrow IPC/Feather) replaced both at runtime; kept only as this
# project's own cross-verification oracle across dozens of tests in both
# modules' own `#[cfg(test)]` blocks. Unlike every other dev-only oracle in
# this file, these two have no way to be made conditional on the `parquet`
# feature the way the *runtime* dependency was - Cargo has no concept of an
# optional dev-dependency - so `cargo test` now always compiles `arrow`/
# `parquet` regardless of which --features are passed, a real, disclosed
# cost given their own documented ~7-9 minute clean-build time (`cargo
# build --release`, the shipped binary, is completely unaffected either way).
arrow = { version = "59.2.0", features = ["ipc", "ipc_compression", "chrono-tz"] }
parquet = { version = "59.2.0", features = ["arrow", "json"] }
# calamine (and chrono, needed only by calamine's own `as_datetime()` API)
# used to be a real, optional runtime dependency of the `xlsx` feature -
# every format it covers (.xlsx/.ods/.xls/.xlsb) now has its own
# hand-rolled reader instead (see CLAUDE.md's Dependency footprint
# section), so both moved here: kept only as this project's own
# cross-verification oracle in the `*_matches_calamine_output_exactly`
# tests (`#[cfg(all(test, feature = "xlsx"))]` in src/lib.rs), the same
# "dev-only, never touches the shipped binary" treatment criterion above
# already gets.
calamine = { version = "0.36.1", features = ["chrono"] }
chrono = "0.4"
# Same dev-only treatment, same reason: the `ini` feature's own hand-rolled
# reader (`ini_support` in src/lib.rs) replaced this at runtime; kept only
# for the `ini_reader_matches_rust_ini_output_exactly` cross-verification
# test.
rust-ini = "0.21.3"
# Same dev-only treatment, same reason: the `xml` feature's own hand-rolled
# reader (`xml_support` in src/lib.rs) replaced this at runtime; kept only
# for the `xml_reader_matches_xmltree_output_exactly` cross-verification
# test. Same `attribute-order` feature as before, so the comparison isn't
# comparing against an artificially-reordered attribute map.
xmltree = { version = "0.12.0", features = ["attribute-order"] }
# Same dev-only treatment, same reason: the `zstd` feature's own hand-rolled
# decoder (`zstd_support` in src/lib.rs - FSE, Huffman, and the full RFC
# 8878 frame/block/literals/sequences pipeline) replaced this at runtime;
# kept only as this project's own cross-verification oracle in
# `zstd_reader_matches_the_zstd_crate_output_exactly`
# (`#[cfg(all(test, feature = "zstd"))]` in src/lib.rs).
zstd = "0.13.3"
# Same dev-only treatment, same reason: the `npy` feature's own hand-rolled
# reader (`npy_support` in src/lib.rs - the NPY header/dtype format, plus
# `zip_support::ZipArchive` for `.npz`) replaced this at runtime; kept only
# as this project's own cross-verification oracle in
# `npy_reader_matches_the_npyz_crate_output_exactly` and
# `npz_reader_matches_the_npyz_crate_output_exactly`
# (`#[cfg(all(test, feature = "npy"))]` in src/lib.rs). The `npz` feature
# is still needed here (unlike at runtime) purely for the oracle's own use
# of `npyz::npz::NpzArchive`.
npyz = { version = "0.9.1", features = ["npz"] }
# Same dev-only treatment, same reason: the `msgpack` feature's own hand-
# rolled decoder (`msgpack_support` in src/lib.rs) replaced it at runtime;
# kept only as this project's own cross-verification oracle in
# `msgpack_reader_matches_the_rmpv_crate_output_exactly`
# (`#[cfg(all(test, feature = "msgpack"))]` in src/lib.rs).
rmpv = "1.3.1"
# Same dev-only treatment, same reason: the `toml` feature's own hand-
# rolled parser (`toml_support` in src/lib.rs) replaced it at runtime;
# kept only as this project's own cross-verification oracle in
# `toml_reader_matches_the_toml_crate_output_exactly`
# (`#[cfg(all(test, feature = "toml"))]` in src/lib.rs).
toml = "1.1.4"
# Same dev-only treatment, same reason: the `cbor` feature's own hand-
# rolled decoder (`cbor_support` in src/lib.rs) replaced it at runtime;
# kept only as this project's own cross-verification oracle in
# `cbor_reader_matches_the_ciborium_crate_output_exactly`
# (`#[cfg(all(test, feature = "cbor"))]` in src/lib.rs).
ciborium = "0.2.2"
# Same dev-only treatment, same reason: the `weblog` and `syslog` features'
# own hand-rolled line parsers (`weblog_support`/`syslog_support` in
# src/lib.rs) replaced this at runtime; kept only as this project's own
# cross-verification oracle in
# `weblog_reader_matches_the_regex_crate_output_exactly` and
# `syslog_reader_matches_the_regex_crate_output_exactly`
# (`#[cfg(all(test, feature = "weblog"))]`/`#[cfg(all(test, feature =
# "syslog"))]` in src/lib.rs).
regex = "1.13.1"
# Same dev-only treatment, same reason: the `dbase` feature's own hand-
# rolled reader (`dbase_support` in src/lib.rs) replaced it at runtime;
# kept only as this project's own cross-verification oracle in
# `dbase_reader_matches_the_dbase_crate_output_exactly`
# (`#[cfg(all(test, feature = "dbase"))]` in src/lib.rs).
dbase = "0.8.0"
# Same dev-only treatment, same reason: the `stata` feature's own hand-
# rolled reader (`stata_support` in src/lib.rs) replaced it at runtime;
# kept only as this project's own cross-verification oracle in
# `stata_reader_matches_the_dta_crate_output_exactly`
# (`#[cfg(all(test, feature = "stata"))]` in src/lib.rs).
dta = "0.6.0"
# Same dev-only treatment, same reason: the `avro` feature's own hand-
# rolled reader (`avro_support` in src/lib.rs - schema parsing, binary
# decoding, and its own Deflate/Snappy/Zstandard codec integration, the
# latter two reusing `inflate`/`zstd_support` rather than duplicating
# them) replaced this at runtime; kept only as this project's own
# cross-verification oracle in
# `avro_reader_matches_the_apache_avro_crate_output_exactly`
# (`#[cfg(all(test, feature = "avro"))]` in src/lib.rs). `num-bigint`
# moves with it - it was only ever depended on directly to decode
# apache-avro's own `Decimal` value type (see that oracle's own
# `avro_decimal_to_string_via_apache_avro_crate`), which no longer runs
# outside tests.
apache-avro = { version = "0.22.0", features = ["snappy", "zstandard"] }
num-bigint = "0.4"
# Same dev-only treatment, same reason: the `sqlite` feature's own hand-
# rolled reader (`sqlite_support` in src/lib.rs - direct B-tree page
# traversal, record decoding, and a small CREATE-TABLE-column-name
# parser) replaced this at runtime; kept only as this project's own
# cross-verification oracle in
# `sqlite_reader_matches_the_rusqlite_crate_output_exactly`
# (`#[cfg(all(test, feature = "sqlite"))]` in src/lib.rs). Unlike every
# other crate in this list, `rusqlite` was never a Rust reimplementation
# of its format to begin with - it's an FFI binding over the real, linked
# SQLite C library - so it still serves as a genuine independent oracle
# (a different codebase entirely, not just a different Rust parser).
rusqlite = { version = "0.40.2", features = ["bundled"] }
# Same dev-only treatment, same reason: the `sas7bdat` feature's own hand-
# rolled reader (`sas7bdat_support` in src/lib.rs - page/subheader
# traversal, RLE/RDC row decompression, and format-driven date/datetime/
# time classification) replaced this at runtime; kept only as this
# project's own cross-verification oracle in
# `sas7bdat_reader_matches_the_sas7bdat_crate_output_exactly`
# (`#[cfg(all(test, feature = "sas7bdat"))]` in src/lib.rs).
sas7bdat = "0.8.0"
# Unlike every other dev-dependency in this list, `ambers` was never a real
# runtime dependency of this project's own `spss` feature to begin with -
# SPSS support went straight to a hand-rolled reader (`spss_support` in
# src/lib.rs, verified directly against `ambers`'s own source) from the
# start (this project's own earlier "No SPSS" decision was about avoiding
# `ambers`'s then-unconditional `arrow` dependency as a *runtime* cost, not
# about the format itself being untractable to hand-roll). Kept the same
# dev-only "never touches the shipped binary" treatment as every other
# oracle here regardless, since it plays the identical role: cross-
# verification in `spss_reader_matches_the_ambers_crate_output_exactly`
# (`#[cfg(all(test, feature = "spss"))]` in src/lib.rs).
ambers = "0.4.2"
# `ambers` itself pins `arrow` 57, a whole major version behind this
# project's own `arrow` 59.2.0 dev-dependency above (kept for the Parquet/
# Arrow IPC oracles) - the two `DataType`/`Array` types aren't the same
# type to the compiler even though they share a name, so the SPSS oracle
# needs its own explicitly-versioned, renamed handle on the exact `arrow`
# `ambers`'s own `RecordBatch` is built from, rather than reusing this
# project's newer one.
arrow57 = { package = "arrow", version = "57", default-features = false }
# Same dev-only treatment, same reason: the `orc` feature's own hand-
# rolled reader (`orc_support` in src/lib.rs - a from-scratch Protobuf
# reader for PostScript/Footer/StripeFooter, plus RLEv1/RLEv2 integer
# decoding) was built and verified directly against `orc-rust`'s own
# source from the start, the same way SPSS went straight to a hand-roll
# once `ambers` stopped being a runtime-weight concern (see that entry
# above) - `orc-rust` was never a runtime dependency of this project's
# `orc` feature either. Kept only for cross-verification in
# `orc_reader_matches_the_orc_rust_crate_output_exactly`
# (`#[cfg(all(test, feature = "orc"))]` in src/lib.rs). `default-features
# = false` drops the crate's own `async` feature (tokio/futures/
# async-trait) this project's synchronous, whole-file-at-a-time usage
# never needs.
orc-rust = { version = "0.8.0", default-features = false }
# `orc-rust` itself pins `arrow` 58.4.0 - yet a third distinct version
# from this project's own 59.2.0 (Parquet/Arrow IPC) and `ambers`'s 57.x
# (SPSS) - so it needs its own explicitly-versioned, renamed handle for
# the same reason `arrow57` does above.
arrow58 = { package = "arrow", version = "58", default-features = false }
# The official MongoDB Rust driver's own BSON crate - never a runtime
# dependency of this project's `bson` feature (see `bson_support` in
# src/lib.rs), kept only as a genuinely independent cross-verification
# oracle in `bson_reader_matches_the_bson_crate_output_exactly`
# (`#[cfg(all(test, feature = "bson"))]`).
bson = "2"
# Apple's own plist format has no single obvious reference crate the way
# most other hand-rolled formats in this project do - `plist` is the
# most widely used pure-Rust reader/writer, kept as a dev-only oracle for
# both the XML and binary (bplist00) variants `plist_support` hand-rolls.
plist = "1"
# Never a runtime dependency of this project's `json5` feature - the
# most widely used pure-Rust JSON5 parser, kept as a dev-only oracle for
# `json5_support`'s own deliberately-narrower (comments/trailing
# commas/unquoted keys/single-quoted strings only) relaxed-JSON parser.
json5 = "0.4.1"
# Never a runtime dependency of this project's `geojson` feature - the
# established, widely used pure-Rust GeoJSON crate, kept as a dev-only
# oracle for `geojson_support`'s own plumbing over the core JSON parser.
# `geo-types` (its own required dependency) comes along for free but is
# never touched directly - this project only ever reads geometry back
# out through `geojson::Value`'s own coordinate accessors.
geojson = "1.0.0"
# Never a runtime dependency of this project's `vcard`/`icalendar`
# features - a real, independent parser for both formats' shared
# property-line grammar (RFC 6350 vCard, RFC 5545 iCalendar both build
# on the same folded `PROPERTY;PARAM=VALUE:value` content-line syntax),
# kept as a dev-only oracle for both `vcard_support` and `ical_support`.
ical = "0.11.0"
# No dev-dependency oracle for `mbox` - the only real candidate on
# crates.io, `mbox-reader 0.2.0`, was checked directly and found to have
# two real bugs of its own (it silently drops the last message in a
# file, and mis-trims a plain LF-terminated envelope line by one byte -
# see CLAUDE.md's Dependency footprint section for the full writeup), so
# trusting it as an oracle would have taught this project's own reader
# to reproduce those bugs just to keep a comparison test green.
# `mbox_support` is instead verified directly against real fixture
# content in `mbox_reader_captures_every_message_including_the_last`.

# harness = false on every target below: Criterion supplies its own `main`,
# replacing the default libtest harness (which doesn't understand
# Criterion's `criterion_group!`/`criterion_main!` macros).
[[bench]]
name = "heuristic_engine"
harness = false

[[bench]]
name = "end_to_end"
harness = false

[[bench]]
name = "format_comparison"
harness = false
required-features = ["parquet", "sqlite", "xlsx"]
