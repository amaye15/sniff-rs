; Test duplicate sections and keys
[owner]
name = Alice
name = Alicia ; duplicate key - should pool into array
email = alice@example.com

[owner]
role = admin ; re-opening same section should append, not create duplicate

[database]
server = 192.168.1.1
port = 5432
[database]
port = 5433 ; duplicate section, same key different value? but ini pools duplicate keys into array
